import { GlobalContainer } from './GlobalContainer';


describe('Global Container Container', () => {
  test('Should call the API and populate product', async () => {
    // arrange

    // act
    const container = new GlobalContainer();


    // assert

  });
});
